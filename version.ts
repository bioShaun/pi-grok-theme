/**
 * version.ts — single source of truth for the displayed extension version.
 *
 * `test/release.test.js` asserts this stays synchronized with the
 * `package.json` version, so bump them together.
 */
export const VERSION = "0.4.0";
